version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - ineffassign
    - misspell
    - staticcheck

formatters:
  enable:
    - gofmt

issues:
  exclude-rules:
    # テストファイルの長い関数を許可
    - path: _test\.go
      text: "Function '.+' is too long"